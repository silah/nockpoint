version: '3.8'

services:
  nockpoint:
    build: .
    ports:
      - "8080:8080"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=your-production-secret-key-here
      # Uncomment below to use PostgreSQL instead of SQLite
      # - DATABASE_URL=postgresql://user:password@postgres:5432/nockpoint
    volumes:
      # Mount volume to persist SQLite database
      - ./instance:/app/instance
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Uncomment this section if you want to use PostgreSQL
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: nockpoint
  #     POSTGRES_USER: nockpoint_user
  #     POSTGRES_PASSWORD: nockpoint_password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# Uncomment if using PostgreSQL
# volumes:
#   postgres_data: